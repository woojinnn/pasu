type SignAuthorizationContext = {
  market?: Pool,
  authorizer: String,
  authorized: String,
  isAuthorized: Bool,
  authorizationScope: String,
  amount?: AmountConstraint,
  nonce?: String,
  validity: Validity,

  authorizedIsContract?: Bool,
  validityDeltaSec?: Long,
};

action "sign_authorization" appliesTo {
  principal: Wallet,
  resource: Protocol,
  context: SignAuthorizationContext,
};
