{
  "methods": {
    "address.activity": {
      "name": "address.activity",
      "description": "Outbound transaction count (account nonce) + first-seen unix timestamp of an arbitrary recipient — the 'brand-new / never-used address?' signal a new-recipient cooldown needs. v1 Ethereum mainnet; env-gated RPC + Etherscan source.",
      "params": {
        "address": {
          "type": "String",
          "required": true,
          "description": "Recipient under review, 0x-hex.",
          "defaultSelector": "$.action.recipient"
        },
        "chain_id": {
          "type": "Long",
          "required": false,
          "defaultSelector": "$.root.chain_id"
        }
      },
      "returns": { "kind": "record", "type": "AddressActivity" },
      "origin": "bundled"
    },
    "address.reputation": {
      "name": "address.reputation",
      "description": "Is an address flagged on a scam/drainer reputation feed (malicious-actor heuristic, distinct from the legal sanctions list)? Env-gated source; fail-open when unconfigured.",
      "params": {
        "address": {
          "type": "String",
          "required": true,
          "description": "Counterparty under review (claim distributor / swap-output token / approval spender / representative), 0x-hex."
        },
        "chain_id": {
          "type": "Long",
          "required": false,
          "defaultSelector": "$.root.chain_id"
        }
      },
      "returns": { "kind": "scalar", "type": "Bool", "from": "$.result.flagged" },
      "origin": "bundled"
    },
    "address.similarity": {
      "name": "address.similarity",
      "description": "Does the action's recipient share leading/trailing hex with an address the wallet already knows (held-token contract / approval spender) but differ from it — the address-poisoning look-alike trap?",
      "params": {
        "candidate": {
          "type": "String",
          "required": true,
          "description": "Recipient under review, 0x-hex.",
          "defaultSelector": "$.action.recipient"
        },
        "chain_id": {
          "type": "Long",
          "required": false,
          "defaultSelector": "$.root.chain_id"
        }
      },
      "returns": { "kind": "scalar", "type": "Bool", "from": "$.result.poisonCollision" },
      "origin": "bundled"
    },
    "lending.health_factor": {
      "name": "lending.health_factor",
      "description": "The wallet's predicted POST-action health factor for the lending market the action targets (a borrow/withdraw lowers it), read from the reducer's predicted state.",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": false,
          "defaultSelector": "$.root.chain_id"
        },
        "owner": {
          "type": "String",
          "required": false,
          "defaultSelector": "$.root.from"
        },
        "venue": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.action.venue"
        },
        "asset": {
          "type": "AssetRef",
          "required": false,
          "defaultSelector": "$.action.asset"
        },
        "amount": {
          "type": "String",
          "required": false,
          "defaultSelector": "$.action.amount"
        }
      },
      "returns": { "kind": "scalar", "type": "Decimal", "from": "$.result.postActionHf" },
      "origin": "bundled"
    },
    "pool.liquidity": {
      "name": "pool.liquidity",
      "description": "An AMM pool/venue's rolling 24h USD trading volume — a liquidity-depth proxy that catches a too-thin-to-exit pool. Env-gated analytics source.",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": true,
          "defaultSelector": "$.root.chain_id"
        },
        "venue": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.action.venue"
        }
      },
      "returns": { "kind": "scalar", "type": "Decimal", "from": "$.result.vol24hUsd" },
      "origin": "bundled"
    },
    "address.sanctions": {
      "name": "address.sanctions",
      "description": "Is an address on an on-chain sanctions oracle (Chainalysis isSanctioned — OFAC/EU/UN)? Legal-list membership, distinct from a scam/drainer heuristic. v1 screens Ethereum mainnet (the EigenLayer chain).",
      "params": {
        "address": {
          "type": "String",
          "required": true,
          "description": "Address under review (delegation operator / newOperator / counterparty), 0x-hex."
        },
        "chain_id": {
          "type": "Long",
          "required": false,
          "defaultSelector": "$.root.chain_id"
        }
      },
      "returns": { "kind": "scalar", "type": "Bool", "from": "$.result.sanctioned" },
      "origin": "bundled"
    },
    "approval.allowance": {
      "name": "approval.allowance",
      "description": "Inspect a token allowance: surfaces raw value, unlimited-allowance flag, and coverage of a requested amount.",
      "params": {
        "allowance": {
          "type": "String",
          "required": false,
          "description": "Current allowance (uint256 string). Defaults to 0 when omitted."
        },
        "requested_amount": {
          "type": "String",
          "required": false,
          "description": "Amount being requested by the transaction (uint256 string)."
        }
      },
      "returns": {
        "kind": "scalar",
        "type": "Bool",
        "from": "$.result.coversRequestedAmount"
      },
      "origin": "bundled"
    },
    "approval.cover_inputs": {
      "name": "approval.cover_inputs",
      "description": "Whether the token allowance covers the requested input amount.",
      "params": {
        "allowances_cover_inputs": {
          "type": "Bool",
          "required": false,
          "description": "Caller-supplied override; takes precedence when present."
        },
        "allowance": {
          "type": "String",
          "required": false,
          "description": "Current allowance (uint256 string)."
        },
        "requested_amount": {
          "type": "String",
          "required": false,
          "description": "Amount being requested by the transaction."
        }
      },
      "returns": {
        "kind": "scalar",
        "type": "Bool",
        "from": "$.result.allowancesCoverInputs"
      },
      "origin": "bundled"
    },
    "clock.now": {
      "name": "clock.now",
      "description": "Current Unix timestamp (seconds) from the daemon's clock.",
      "params": {},
      "returns": { "kind": "scalar", "type": "Long", "from": "$.result.nowTs" },
      "origin": "bundled"
    },
    "oracle.effective_rate_bps": {
      "name": "oracle.effective_rate_bps",
      "description": "Effective rate of a swap vs the oracle's mid-market price, expressed in basis points (negative = unfavourable).",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": true,
          "defaultSelector": "$.root.chain_id"
        },
        "token_in": {
          "type": "AssetRef",
          "required": true,
          "defaultSelector": "$.action.inputToken.asset"
        },
        "amount_in": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.action.inputToken.amount.value"
        },
        "token_out": {
          "type": "AssetRef",
          "required": true,
          "defaultSelector": "$.action.outputToken.asset"
        },
        "amount_out": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.action.outputToken.amount.value"
        }
      },
      "returns": { "kind": "scalar", "type": "Long", "from": "$.result.bps" },
      "origin": "bundled"
    },
    "oracle.usd_value": {
      "name": "oracle.usd_value",
      "description": "Convert a token amount to its USD valuation via a price oracle.",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": true,
          "description": "EIP-155 chain ID.",
          "defaultSelector": "$.root.chain_id"
        },
        "asset": {
          "type": "AssetRef",
          "required": true,
          "description": "Token to price (address, symbol, decimals, …).",
          "defaultSelector": "$.action.inputToken.asset"
        },
        "amount": {
          "type": "String",
          "required": true,
          "description": "On-chain amount (uint256 wei-form string).",
          "defaultSelector": "$.action.inputToken.amount.value"
        },
        "source": {
          "type": "String",
          "required": false,
          "description": "Which price source to query. `coingecko` is HTTP-based and works out of the box; `chainlink` reads on-chain feeds and needs POLICY_RPC_CHAIN_RPCS configured per chain.",
          "enum_": ["coingecko", "chainlink"],
          "default": "coingecko"
        }
      },
      "returns": { "kind": "record", "type": "UsdValuation" },
      "origin": "bundled"
    },
    "portfolio.balance": {
      "name": "portfolio.balance",
      "description": "Wallet balance of a specific token.",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": true,
          "defaultSelector": "$.root.chain_id"
        },
        "owner": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.root.from"
        },
        "asset": {
          "type": "AssetRef",
          "required": true,
          "defaultSelector": "$.action.inputToken.asset"
        }
      },
      "returns": { "kind": "scalar", "type": "String", "from": "$.result.balance" },
      "origin": "bundled"
    },
    "portfolio.input_fraction_bps": {
      "name": "portfolio.input_fraction_bps",
      "description": "Fraction of the wallet's portfolio (denominated in the input asset) the transaction is spending, in basis points.",
      "params": {
        "chain_id": {
          "type": "Long",
          "required": true,
          "defaultSelector": "$.root.chain_id"
        },
        "owner": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.root.from"
        },
        "asset": {
          "type": "AssetRef",
          "required": true,
          "defaultSelector": "$.action.inputToken.asset"
        },
        "amount": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.action.inputToken.amount.value"
        }
      },
      "returns": { "kind": "scalar", "type": "Long", "from": "$.result.bps" },
      "origin": "bundled"
    },
    "dambi.evaluate_v3": {
      "name": "dambi.evaluate_v3",
      "description": "Phase 5D mock — echo (actions, eval_context, wallet_id) back as policyRequest.actions with empty state_before / deltas / state_after. Real implementation arrives in Phase 6 (reducer + state sync).",
      "params": {
        "wallet_id": {
          "type": "String",
          "required": true,
          "description": "WalletId (address + chains). Carried verbatim by the mock; Phase 6 will key state persistence off this."
        },
        "actions": {
          "type": "String",
          "required": true,
          "description": "List of caller-built Actions (typed PDF FSM ActionBody plus ActionMeta). The mock echoes these into policyRequest.actions."
        },
        "eval_context": {
          "type": "String",
          "required": true,
          "description": "EvalContext (chain + clock + RequestKind + SimulationMode + action_index)."
        }
      },
      "returns": { "kind": "record", "type": "UsdValuation" },
      "origin": "bundled"
    },
    "stat_window.snapshot": {
      "name": "stat_window.snapshot",
      "description": "Rolling-window snapshot of recent on-chain activity for the wallet.",
      "params": {
        "owner": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.root.from"
        }
      },
      "returns": { "kind": "record", "type": "WindowStats" },
      "origin": "bundled"
    },
    "stat_window.swap_stats": {
      "name": "stat_window.swap_stats",
      "description": "Per-action stats over a sliding 24h window (volume + count).",
      "params": {
        "owner": {
          "type": "String",
          "required": true,
          "defaultSelector": "$.root.from"
        },
        "action": {
          "type": "String",
          "required": true,
          "description": "Action keyword (e.g. \"swap\").",
          "defaultSelector": "swap"
        }
      },
      "returns": { "kind": "record", "type": "WindowStats" },
      "origin": "bundled"
    }
  }
}
