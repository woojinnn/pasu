type ClaimUnstakeContext = {
  tokenOut: AssetRef,
  amountOut?: AmountConstraint,

  recipient: String,
  custom?: ClaimUnstakeCustomContext,
};

type ClaimUnstakeCustomContext = {};

action "claim_unstake" appliesTo {
  principal: Wallet,
  resource: Protocol,
  context: ClaimUnstakeContext,
};
