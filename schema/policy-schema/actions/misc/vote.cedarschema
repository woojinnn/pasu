type VoteContext = {
  governance: String,

  proposalId: String,
  support: String,
  reason?: String,

  validity?: Validity,
  custom?: VoteCustomContext,
};

type VoteCustomContext = {};

action "vote" appliesTo {
  principal: Wallet,
  resource: Protocol,
  context: VoteContext,
};
