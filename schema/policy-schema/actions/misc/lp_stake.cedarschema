type LpStakeContext = {
  gauge: String,
  lpToken: AssetRef,
  amount: AmountConstraint,
  recipient: String,
};

action "lp_stake" appliesTo {
  principal: Wallet,
  resource: Protocol,
  context: LpStakeContext,
};
