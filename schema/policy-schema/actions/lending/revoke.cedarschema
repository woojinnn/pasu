type RevokeContext = {
  target?: Pool,
  caller: String,
  subject: String,
  revokeKind: String,
  custom?: RevokeCustomContext,
};

type RevokeCustomContext = {};

action "revoke" appliesTo {
  principal: Wallet,
  resource: Protocol,
  context: RevokeContext,
};
