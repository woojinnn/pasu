{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schema/actions/decrease_liquidity.json",
  "title": "DecreaseLiquidityAction",
  "description": "Uniswap V3 NPM.decreaseLiquidity 또는 Uniswap V4 PositionManager 의 DECREASE_LIQUIDITY action 한정 — 기존 ERC-721 position 의 internal liquidity 를 감소시키는 동작. V3 의 경우 결과 토큰이 NFT 의 tokensOwed 에 적립되며 wallet 자산은 즉시 변하지 않음 (회수는 별도 collect 가 필요). V4 의 경우 같은 action 안에서 TAKE 까지 묶여 atomic 으로 wallet 으로 옴.",

  "type": "object",
  "properties": {
    "nft": {
      "$ref": "../common/_common.json#/$defs/AssetRef",
      "description": "Position 을 보유한 NFT collection (kind=erc721). address = V3 NPM 또는 V4 PositionManager."
    },

    "tokenId": {
      "$ref": "../common/_common.json#/$defs/DecimalString",
      "description": "기존 position 의 tokenId. V3 DecreaseLiquidityParams.tokenId / V4 DECREASE_LIQUIDITY action params. NPM 은 msg.sender == owner 또는 approved 검증."
    },

    "liquidityDelta": {
      "$ref": "../common/_common.json#/$defs/AmountConstraint",
      "description": "감소시킬 internal liquidity 양 (uint128, 양수 표현). V3 DecreaseLiquidityParams.liquidity / V4 modifyLiquidities params 의 liquidityDelta (calldata 에서는 음수 전달이지만 schema 는 절대값). kind=exact."
    },

    "outputs": {
      "type": "array",
      "items": { "$ref": "../common/_common.json#/$defs/AssetRef" },
      "minItems": 2,
      "maxItems": 2,
      "description": "회수 예정 토큰 2개 (token0, token1). V3 는 tokensOwed 적립, V4 는 wallet 으로 즉시 송금."
    },

    "minAmountsOut": {
      "type": "array",
      "items": { "$ref": "../common/_common.json#/$defs/AmountConstraint" },
      "minItems": 2,
      "maxItems": 2,
      "description": "outputs[i] 와 1:1 매핑하는 최소 회수량 (슬리피지 보호). V3 의 amount0Min / amount1Min, V4 의 amount0Min / amount1Min (kind=min)."
    },

    "validity": {
      "$ref": "../common/_common.json#/$defs/Validity",
      "description": "유효기간 — source='tx-deadline'. V3 DecreaseLiquidityParams.deadline. V4 는 wrapper deadline."
    }
  },

  "required": ["nft", "tokenId", "liquidityDelta", "outputs", "minAmountsOut"]
}
