{
  "name": "scopeball-registry",
  "version": "0.2.0",
  "description": "ScopeBall Adapter Loader registry v2 — production",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build-index.ts",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "canonicalize": "^2.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
