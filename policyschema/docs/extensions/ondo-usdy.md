# `ondo.usdy` Extension

Ondo Finance USDY — KYC-게이트 단기 국채 yield 토큰.

| 진입점 | 주소 |
|---|---|
| USDY | `0x96F6eF951840721AdBF46Ac996b59E0235CB985C` |
| Allowlist | `0x...` (KYC gating) |

매핑: ActionType `Subscribe`/`RequestRedemption`/`TransferRestricted`. KYC 검증 + receipt mint.

v0.1 *세미-어댑터 미구현*.
