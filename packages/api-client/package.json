{
  "name": "@scopeball/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed HTTP client for the scopeball policy-rpc server. Shared by apps/web and apps/dashboard (and the extension dashboard while it lives under browser-extension/).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@scopeball/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
