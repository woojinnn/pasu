{
  "name": "pasu",
  "private": true,
  "description": "Pasu monorepo — Rust backend (crates/), browser extension. Frontend lives in browser-extension/dashboard/ for now.",
  "packageManager": "yarn@4.14.1",
  "scripts": {
    "server:local": "bash scripts/start-policy-server.sh local",
    "server:ext": "bash scripts/start-policy-server.sh ext"
  }
}
