{
  "label": "eth_sign (deprecated) — opaque 32-byte hash",
  "method": "eth_sign",
  "chain_id": 1,
  "params": [
    "0x7ee04c7057ad92b7dcc8e9bb26358c6b0a62822c",
    "0xdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeefdeadbeef"
  ],
  "notes": "eth_sign accepts a raw 32-byte hash with no domain or structure. Most wallets disable it by default because the hash could correspond to *any* signed message — including a transaction. mappers/sign emits a `sign_message` envelope with `messageDigest` equal to the input hash; the policy layer should typically *block* this method outright."
}
