[package]
name = "policy-transition"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
policy-action = { workspace = true }
policy-state = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
# Tsify-next derives TS bindings for every pub Action / ActionBody type.
tsify-next = { workspace = true }
wasm-bindgen = { workspace = true }
# Fixed-point decimal arithmetic for `helpers/derived.rs` (HF / LTV / PnL).
rust_decimal = { workspace = true }
