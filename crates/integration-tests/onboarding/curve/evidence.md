# Curve Protocol Onboarding Evidence

> Re-onboarding run for Curve. Treat existing Curve artifacts as candidates, not
> proof. Fill each phase row with exact commands, counts, artifacts, and blockers.

## Run Metadata

| field | value |
|---|---|
| protocol | curve |
| branch | feat/curve-onboarding-redo |
| worktree | /Users/jhy/Desktop/ScopeBall/scopeball-registry-v2 |
| date | 2026-06-01 |
| main agent | Codex |
| base commit | e2bb86d2 |

## P0 Research Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| Codex current-session research executed | done | Codex fetched Curve public API snapshots with `curl -L -s https://api.curve.fi/api/getPools/{all,ethereum/main,ethereum/factory,ethereum/factory-stable-ng,ethereum/crypto,ethereum/factory-crypto,ethereum/factory-tricrypto,ethereum/factory-twocrypto}` into `/private/tmp/curve-*.json`, compared them with `registryV2/surface/curve/_deployments.json`, and wrote `crates/integration-tests/onboarding/curve/address-universe-summary.json`. |
| Claude Code or sub-agent research executed | done | Sub-agent `019e817a-891d-7d73-819c-40117dde92f0` reviewed contract/pool universe local artifacts; sub-agent `019e817a-a93e-76b1-adb1-8675f11b19db` reviewed token-surface local artifacts; sub-agent `019e81c6-3973-7e33-ac2e-f7d1ce0bf6aa` reviewed Curve source-resolver/materialization design. Claude Code CLI was attempted with `claude -p ...` but blocked with `Not logged in · Please run /login`; sub-agents satisfied the framework's required independent second opinion for this run. |
| Claude/sub-agent exact prompt or command recorded | done | Prompts are recorded in this session transcript. Claude command attempted: `claude -p --permission-mode auto --allowedTools Read,Grep,Glob -- "<Curve P0 second-opinion prompt>"`; result `Not logged in`. Sub-agent prompts included cwd, branch, read targets, no-edit guardrail, pool/factory universe focus, token-surface focus, and gap classification output. |
| Codex-only candidates listed | done | `address-universe-summary.json` recorded the initial Ethereum Curve API `all` universe: 2,265 pool rows / 2,260 unique pool addresses. The checked-in expanded universe is now `registryV2/surface/curve/_pool_universe.json`: 3,097 Ethereum/Base pool candidates. Top candidates include 3Crv `0xbebc...ff1c7`, PYUSDUSDS `0xa632...fc1f`, steCRV `0xdc24...7022`, and Base factory pools. |
| Claude/sub-agent-only candidates listed | done | Contract-universe sub-agent identified local-only gaps: `_deployments.json` is a selected concrete subset; factory contracts are excluded without a recorded factory/event/address-provider universe query/count; no `chain_to_addresses_source` resolver for Curve. Token sub-agent identified local-only token-gate gaps and four token files lacking `token_kind` despite having `erc_kind`. |
| dropped-unverified candidates listed with reason | done | No hallucinated candidate was accepted. Existing `_deployments.json` still warns prior LLM cross-check addresses were hallucinated and not accepted. Current Curve API candidates are not silently dropped: every pool/gauge candidate is `cover` or `defer` in `_pool_universe.json` / `_gauge_universe.json`. |
| final contract inventory verified against first-party sources | blocked | Full Curve support is not complete, but the pool/gauge universe is now explicit and machine-readable. `_pool_universe.json` has 3,097 pool candidates with every row dispositioned; `_gauge_universe.json` has 1,219 gauge candidates with every row dispositioned. `_deployments.json` remains the selected concrete surface for ABI/coverage gates. |
| pool-heavy/factory protocol address universe source/query/count recorded, or explicitly not applicable | done | Re-fetched Curve public API snapshots into `/private/tmp/curve-*-p0.json` and generated `registryV2/surface/curve/_pool_universe.json`. Sources include Ethereum `all/main/factory/factory-stable-ng/crypto/factory-crypto/factory-tricrypto/factory-twocrypto` and Base `all/factory-stable-ng/factory-tricrypto`. Unique candidates: 3,097 pools (Ethereum 2,260, Base 837). |
| pool-heavy/factory universe artifact is machine-readable, nonzero, and committed, or explicitly not applicable | done | `registryV2/surface/curve/_pool_universe.json`: `source_count=3097`, `counts.total=3097`, `cover=1203`, `defer=1894`, `exclude=0`. `registryV2/surface/curve/_gauge_universe.json`: `source_count=1219`, `counts.total=1219`, `cover=1219`, `defer=0`, `exclude=0`. |
| every pool/factory child address in universe dispositioned as cover/exclude/defer with reason and batch boundary | done | `_pool_universe.json` dispositions every candidate: 16 existing concrete supported pools plus 1,187 StableSwap-NG 2-coin pools are `cover`; 1,894 remaining pools are `defer` with explicit family batches (`factory-crypto`, `factory-twocrypto`, `factory-tricrypto`, legacy main/factory/crypto, Base tricrypto/unclassified). `_gauge_universe.json` dispositions all 1,219 gauge candidates as source-covered. |
| concrete manifest vs protocol source resolver/generator strategy decided for pool universe | done | Current batch adds protocol-agnostic source materialization: `curve:factory_stable_ng_2coin_mainnet`, `curve:factory_stable_ng_2coin_base`, and address-only `curve:gauges`. StableSwap-NG 2-coin pools are generated from source manifests with `$source.*` context; remaining pool families are deferred until family-specific materializers/ABI templates are added. |
| `npm run check:universe -- --protocol <protocol>` output recorded for pool/factory/vault-heavy protocols, or explicitly not applicable | done | `cd registryV2 && npm run check:universe -- --protocol curve --require-cover-linkage` PASS: `_gauge_universe.json: 1219 candidates · 1219 cover · 0 exclude · 0 defer · source_count=1219`; `_pool_universe.json: 3097 candidates · 1203 cover · 0 exclude · 1894 defer · source_count=3097`. |
| token-surface inventory completed or explicitly scoped out | blocked | Covered StableSwap-NG 2-coin batch generated 1,733 token registry files: LP/share tokens plus underlying coins for promoted Ethereum/Base pools. Full Curve token surface is still blocked for the 1,894 deferred pool candidates and by the absence of a protocol-agnostic token completeness gate. |
| `registryV2/surface/<protocol>/_deployments.json` updated if applicable | done | `_deployments.json` remains the concrete ABI/coverage surface. Pool/gauge universe expansion is represented in source-consumed artifacts `_pool_universe.json` and `_gauge_universe.json`; this avoids adding thousands of factory child pools to the concrete coverage file while preserving machine disposition/linkage gates. |
| `npm run check:surface` output recorded | done | `cd registryV2 && npm run check:surface` PASS. Curve selected subset lines include crvUSD Controller 3 pools, CryptoSwap-NG 5 pools, LiquidityGauge 8 pools, RouterNG 3 pools, StableSwap-NG mainnet 3 pools + Base 1 pool, Twocrypto-NG 8 pools; I0 `curve: 47 deployed · 41 cover · 6 exclude`. This does not prove full Curve pool universe closure. |

## P1 Authoring Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| every COVER selector mapped to existing ActionBody or Tier3 requirement | done | Current Curve batch maps covered selectors to existing ActionBody domains only. Added source-materialized StableSwap-NG 2-coin swap/add/remove-liquidity manifests map to `amm`; added sourced gauge deposit/withdraw/claim manifests map to `staking`. Existing concrete Curve manifests still map to `amm`, `lending`, `staking`, and `permission`; no new Tier3 schema was introduced. |
| permission/fund-movement/red-flag selector review recorded | done | `registryV2/surface/curve/*.coverage.json` records all external mutating selectors as cover/exclude. Red-flag permission selectors are covered either through protocol-specific `permission` manifests (`llamalend approve`, `minter toggle_approve_mint`) or excluded only when they are standard ERC20 pool/LP token functions covered by standard token auto-enumeration. Fund movement selectors map to AMM swap/add/remove liquidity, lending borrow/repay/collateral, staking deposit/withdraw/claim/veCRV lock, or explicit defer/exclude reasons. |
| manifest files added/changed listed | done | Added 28 sourced Curve manifests: 20 StableSwap-NG 2-coin source templates under `registryV2/manifests/curve/stableswap-ng/zz-source-factory-stable-ng-2coin-{mainnet,base}/` and 8 gauge source templates under `registryV2/manifests/curve/gauge/zz-sourced/`. Added protocol source materialization support in `registryV2/scripts/build-index.ts` and Curve resolvers in `registryV2/scripts/resolvers/curve.ts`. |
| enrichment/live_field decision recorded for every COVER action | done | Source-materialized StableSwap-NG manifests are decode-faithful and inject per-pool token maps through build-time `$source.*` context. Gauge source manifests are decode-faithful staking actions. Existing concrete Curve manifests retain their previous `live_inputs` decisions; no new runtime live/enrichment source was introduced for the materialized batch. |
| required remote policy-RPC/live/enrichment methods have local handler, configured endpoint test, or explicit blocker | done | New sourced Curve manifests declare no `requires.host_capabilities` and no policy-RPC method requirements. Build-time source context comes from checked-in P0 universe artifacts, not runtime policy RPC. Existing concrete Curve live inputs remain unchanged. |
| Tier3 not needed or full Tier3 downstream contract completed | done | No new Tier3 action/domain was needed for this P1 pass. Existing Curve manifests target already-defined domains/actions in `crates/simulation/reducer/src/action/{amm,lending,staking,permission}` and existing `policy-engine/src/lowering_v2/{amm,lending,staking,permission}` lowering paths. |
| Tier3 files listed if applicable: ActionBody/effect/view/sync/lowering_v2/cedarschema/schema registration/conformance test | done | Applicable existing downstream files: `crates/simulation/reducer/src/action/amm`, `crates/simulation/reducer/src/action/lending`, `crates/simulation/reducer/src/action/staking`, `crates/simulation/reducer/src/action/permission`, `crates/policy-engine/src/lowering_v2/{amm,lending,staking,permission}`, and `schema/policy-schema/actions/{amm,lending,staking,permission}`. No new files changed for Tier3 in this run. |
| `npm run check:manifest` or protocol-filtered validate output recorded | done | `cd registryV2 && npm run check:manifest` PASS: build-index wrote `28373 callkey(s) + 82 typed-data entry(ies) across 509 manifest(s)`; Rust v3 harness `validate (all): 28183 single_emit manifest(s) OK, 0 structural errors [iters/manifest=24]`. Protocol-filtered source validate also PASS: `cargo run -p policy-engine-integration-tests --bin v3-harness -- validate --filter curve/stableswap-ng/source --iterations 24` => `11860 single_emit manifest(s) OK, 0 structural errors`. |

## P2 Synthetic Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| fuzz command with seed recorded | done | Existing concrete Curve fuzz: `cargo run -p policy-engine-integration-tests --bin v3-harness -- fuzz --filter curve --iterations 5000 --seed 0xC0FFEE --json /private/tmp/curve-fuzz-5000.json`. New source-materialized batch fuzz: `cargo run -p policy-engine-integration-tests --bin v3-harness -- fuzz --filter curve/stableswap-ng/source --iterations 20 --seed 0xC0FFEE --json /private/tmp/curve-stableswap-ng-source-fuzz-20.json`. |
| iterations >= 5000 or justified lower bound | done | Existing concrete Curve run used 5,000 iterations per entry (`total=1640000 pass=1224146 soft=415854 fail=0 panicked=0 skipped=0`). New source-materialized StableSwap-NG batch used a justified lower bound because the source expands to 11,860 single_emit callkeys; result `total=237200 pass=140030 soft=97170 fail=0 panicked=0 skipped=0`. |
| fixed edge-case matrix recorded | done | `v3-harness` uses deterministic edge/boundary cases before random fuzz cases. Source-materialized batch command above covered `curve/stableswap-ng/source` across 11,860 callkeys with zero hard failures/panics. Existing concrete smoke also passed: `--iterations 4 --seed 0xC0FFEE --json /private/tmp/curve-fuzz-4.json`, `total=1312 fail=0 panicked=0`. |
| permission/value/nested/array/opcode/deadline/path edge coverage recorded | done | Current generated by-callkey surface remains `single_emit` for the new Curve source batch. Synthetic fuzz covers value/ABI boundary cases for StableSwap-NG `amm` actions and gauge `staking` actions; Router path semantics remain covered by real corpus `curve-router-ng/corpus.json`. No new Curve `array_emit`/`opcode_stream_dispatch` route was introduced. |
| representative pass/error corpus entries committed or justified | done | Existing Curve real-tx corpus entries now include `expect_body` field-level semantic assertions for action, venue address, token, amount, recipient, and pool/controller/gauge-specific fields across StableSwap-NG, CryptoSwap-NG, Gauge, veCRV, Minter, crvUSD Controller, RouterNG, and Twocrypto. |

## P2 Real-Tx Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| Etherscan MCP/API availability checked | done | Etherscan REST API v2 reachable with local `.env` key. Initial run exposed a framework/dogfood script bug (`status` vs `_deployments.json` `decision` field produced 0 addresses); corrected run succeeded. |
| Etherscan txlist pull executed adapter-blind by P0 cover addresses | done | `crates/integration-tests/onboarding/curve/etherscan-cover-tx-summary.json`: queried every current local Curve `decision=cover`, `chainId=1` address from `_deployments.json` using `account.txlist`, `offset=10000`, `sort=desc`; 40 cover addresses queried. |
| external tx pull target address count is nonzero and recorded | done | `etherscan-cover-tx-summary.json`: `coverAddressesQueried=40`. The initial zero-target run was discarded and recorded as a dogfood failure (`status` vs `decision` field mismatch), not accepted as evidence. |
| Etherscan `api_calls_used` recorded | done | `etherscan-cover-tx-summary.json`: `apiCalls=40`. |
| Etherscan `raw_txs_seen` recorded | done | `etherscan-cover-tx-summary.json`: `rawTxs=125253`. This exceeds the framework's 10,000 tx/protocol target for the currently selected cover set, but not for the full Curve pool universe. |
| Etherscan `unique_selectors_seen` recorded | done | `etherscan-cover-tx-summary.json`: `uniqueSelectors=112`; top selectors include `0x5c9c18e2`, `0x1e83409a`, `0xdd171e7c`, `0x6a627842`, `0xd7136328`. |
| Etherscan real tx coverage per COVER selector recorded | done | `etherscan-cover-tx-summary.json` records per-address selector counts plus `indexed` flags; `matchedCallkeys=183`. High-volume unmatched address/selectors are also listed, e.g. `0x4ebd...4a14/0x65b2489b` and crvUSD/LlamaLend controller selectors. |
| pool-heavy/factory protocols swept candidate/universe addresses, not only selected cover addresses, or explicitly not applicable | blocked | Not complete. `_pool_universe.json` and `_gauge_universe.json` now provide candidate universes, but Etherscan/Dune real-tx sweep has not yet been rerun over the 3,097 pool candidates or 1,219 gauge candidates. Previous Etherscan sweep covered only 40 local cover/deployment addresses. |
| unknown to-addresses with known protocol selectors bucketed as P0/P2 hard gaps | blocked | Current real-tx summary buckets unknown selectors on known covered addresses only. Need a fresh P2 sweep against `_pool_universe.json` and `_gauge_universe.json` to bucket candidate addresses as `cover`/`defer` and identify any non-universe addresses with known Curve selectors. |
| Dune MCP/API availability checked | done | `mcp__dune.getUsage` succeeded before and after query. Plan `community_fluid_engine_v2`, quota 2,500 credits. |
| Dune usage baseline recorded | done | `crates/integration-tests/onboarding/curve/dune-calibration-summary.json`: before query `creditsUsed=377.867`, after query `creditsUsed=378.16`. |
| Dune calibration/query executed with partition WHERE or explicitly blocked | done | Dune query `7626385` (`https://dune.com/queries/7626385`) executed on free engine with `ethereum.transactions WHERE block_date >= current_date - interval '7' day`, scoped to current local Curve cover addresses. |
| Dune `executionCostCredits` / usage delta recorded | done | `dune-calibration-summary.json`: `executionCostCredits=0.293`, usage delta `0.293`. |
| Dune rows returned / selected tx hashes recorded | done | `dune-calibration-summary.json`: 58 total address/selector rows, 20 preview rows, selected sample hashes recorded for top rows. This is calibration/selector stats, not full Curve universe completion. |
| representative real-tx corpus/golden entries committed or justified | done | `expect_body` semantic assertions were added to all 10 existing Curve pass corpus entries. This makes the existing representative real-tx corpus strict enough to catch misdecoded action/token/amount/venue drift. |
| protocol-filtered corpus replay executed with semantic pin gate: `v3-harness corpus --filter <protocol> --require-expect-body` | done | `cargo run -p policy-engine-integration-tests --bin v3-harness -- corpus --filter curve --require-expect-body --json /private/tmp/curve-corpus-pinned-outcomes.json` PASS: `10/10 matched`, `semantic expect_body: 10/10 pass entries pinned`. |

## P3 Develop Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| all P2 hard/soft/misdecoded/unknown_protocol_address/excluded gaps bucketed | done | Buckets from this run: `CURVE-SOURCE-MATERIALIZE` address-only source was insufficient for pool metadata and was fixed with build-time per-address context materialization; `CURVE-GAUGE-SOURCE` gauge addresses were moved from selected concrete support to a source-backed universe; `P2-SYNTH-SOFT` tolerated `build_action_body_failed` soft cases from boundary/random value-map inputs; `P2-UNIVERSE-REALTX` full pool/gauge universe real-tx sweep remains blocked. No hard synthetic failures, panics, or corpus mismatches after rerun. |
| each fix tied to a gap id, selector, tx hash, or synthetic seed | done | `CURVE-SOURCE-MATERIALIZE` fixed by `source_materialize` and Curve source resolvers for `curve:factory_stable_ng_2coin_{mainnet,base}`; `CURVE-GAUGE-SOURCE` fixed by `curve:gauges` and 8 sourced gauge manifests; synthetic rerun seed `0xC0FFEE`. `P2-UNIVERSE-REALTX` remains tied to 3,097 pool candidates and 1,219 gauge candidates. |
| manifest/decoder/Tier3/harness change list recorded | done | Manifest/build changes: 28 sourced Curve manifests, `registryV2/scripts/resolvers/curve.ts`, build-index `source_materialize`, Curve source manifest/token generators, `check-address-universe` multi-universe scanning, build-index unit test, and docs. No decoder, ActionBody, lowering_v2, Cedar schema, WASM export, or browser-extension runtime file changed. |
| P2 rerun after fixes recorded | done | Strict corpus PASS: `cargo run -p policy-engine-integration-tests --bin v3-harness -- corpus --filter curve --require-expect-body` => `10/10 matched`, `10/10 pass entries pinned`. Source-materialized validate PASS: `validate --filter curve/stableswap-ng/source --iterations 24` => `11860 single_emit manifest(s) OK`. Source-materialized fuzz PASS: `fuzz --filter curve/stableswap-ng/source --iterations 20 --seed 0xC0FFEE` => `fail=0 panicked=0`. |
| corpus `expect` flips or exclusions justified | done | No `expect` values were flipped and no corpus entries were excluded. Existing `expect:"pass"` entries stayed pass; only `expect_body` field-level assertions were added. |
| remaining gaps have explicit defer/blocker disposition | done | Remaining blockers are explicit: 1,894 Curve pool candidates are deferred/not supported; full Curve token surface for deferred pools lacks a machine gate; Claude Code remains unauthenticated; full 3,097-pool / 1,219-gauge universe real-tx sweep is not run. |

## P4 Land Evidence

| required evidence | status | artifact / exact command / summary |
|---|---|---|
| `registryV2 npm run build` output recorded | done | `cd registryV2 && npm run build` PASS: build-index wrote `28373 callkey(s) + 82 typed-data entry(ies) across 509 manifest(s)`. Generated local index file count: `find registryV2/index/by-callkey -name '*.json' -type f | wc -l` => `28317`; typed-data count `82`. |
| registryV2 build-index vitest output recorded | done | Sandbox run failed with tsx IPC `listen EPERM`; escalated rerun PASS: `cd browser-extension && node .yarn/releases/yarn-4.14.1.cjs vitest run --root ../registryV2 scripts/__tests__/build-index.test.ts` => `1 passed`, `12 passed`. |
| `npm run check:manifest` output recorded | done | `cd registryV2 && npm run check:manifest` PASS: build-index wrote `28373 callkey(s) + 82 typed-data entry(ies) across 509 manifest(s)`; Rust v3 harness validate output `28183 single_emit manifest(s) OK, 0 structural errors [iters/manifest=24]`. |
| `npm run check:surface` output recorded | done | `cd registryV2 && npm run check:surface` PASS: `107 gated contracts`; Curve selected subset remains `curve: 47 deployed · 41 cover · 6 exclude`; informational WARNs remain for unrelated protocols without contract-inventory enforcement and 21 ungated protocol contracts. |
| `npm run check:universe -- --protocol <protocol> --require-cover-linkage` output recorded for pool/factory/vault-heavy protocols, or explicitly not applicable | done | `cd registryV2 && npm run check:universe -- --protocol curve --require-cover-linkage` PASS: `_gauge_universe.json: 1219 candidates · 1219 cover · 0 exclude · 0 defer · source_count=1219`; `_pool_universe.json: 3097 candidates · 1203 cover · 0 exclude · 1894 defer · source_count=3097`. |
| v3-harness coverage/fuzz/corpus outputs recorded | done | `v3-harness coverage` PASS: local surface `callkeys=28317 typed_data_keys=82 unique_bundles=12344 install_failures=0`. Source batch fuzz PASS: `/private/tmp/curve-stableswap-ng-source-fuzz-20.json`, `total=237200 pass=140030 soft=97170 fail=0 panicked=0 skipped=0`. Corpus PASS as below. |
| protocol-filtered strict corpus output recorded: `v3-harness corpus --filter <protocol> --require-expect-body` | done | `cargo run -p policy-engine-integration-tests --bin v3-harness -- corpus --filter curve --require-expect-body` PASS: `10/10 matched`, `semantic expect_body: 10/10 pass entries pinned`. |
| `cargo test --workspace` output recorded | done | Sandbox run failed only at `policy-server --test auth_flow` with `PermissionDenied`; escalated rerun PASS for full workspace. Notable totals include `v3_decode_harness: 58 passed`, `policy-engine-wasm: 36 passed / 1 ignored`, `declarative_v3_route: 78 passed`, `policy-server auth_flow: 7 passed`. |
| wasm build output recorded if runtime/wasm/schema changed | done | Not applicable for this checkpoint: no WASM runtime/export/schema crate changed. The change is registry build/materialization logic, manifests, token registry data, source artifacts, docs, and evidence. WASM route compatibility is covered by `npm run check:manifest`, source-filtered validate/fuzz, corpus, and coverage harnesses. |
| fmt/clippy/typecheck output recorded for changed crates/packages | done | `cd registryV2 && npm run typecheck` PASS. `cargo run -p policy-engine-integration-tests --bin v3-harness -- validate --filter curve/stableswap-ng/source --iterations 24` PASS. `cargo run -p policy-engine-integration-tests --bin v3-harness -- coverage` PASS. No Rust source file was changed in this checkpoint. |
| exact staged files and commit hash recorded | done | Staged scope for this phase commit: 1,773 files including registryV2 build-index/resolver/test code, Curve source manifests, Curve pool/gauge universe files, generated Curve token JSON files, protocol-agnostic framework note, and onboarding evidence. Final commit hash is recorded in the session final response. |
| remaining WARNs/deferred selectors/actions listed with reason | done | Curve is substantially expanded but not complete: `_pool_universe.json` explicitly defers 1,894 Curve API pool candidates; full 3,097-pool / 1,219-gauge real-tx sweep is not run; full token surface for deferred pools is not machine-verified; Claude Code is unavailable because CLI is not logged in. Registry surface WARNs for unrelated non-Curve protocols are informational and unchanged. |
| no base/worktree merge performed unless user explicitly requested it | done | No base/worktree merge was performed in this run. Current branch remains `feat/curve-onboarding-redo`; user has not requested merge. |

## Blockers

If a mandatory item cannot be completed, write `blocked` rather than `done`.

| blocker | source | next action |
|---|---|---|
| 1,894 Curve API pool candidates are explicitly deferred, not supported | `registryV2/surface/curve/_pool_universe.json` | Add family-specific materializers/ABI templates for factory-crypto, factory-twocrypto, factory-tricrypto, legacy main/factory/crypto, Base tricrypto, and unclassified API pools to move more deferred pools into `cover`. |
| full Curve token surface not machine-verified | token sub-agent review + missing `check:tokens` gate | Add protocol-agnostic token inventory checker; ensure covered pools/gauges/controllers imply required LP/share/receipt/underlying token JSON or explicit defer. Covered StableSwap-NG 2-coin batch token JSON files were generated in this checkpoint. |
| full Curve universe real-tx sweep not rerun | `_pool_universe.json`, `_gauge_universe.json` | Run Etherscan/Dune adapter-blind sweeps over 3,097 pool candidates and 1,219 gauge candidates, then bucket unknown selectors/address gaps as P0/P2 hard gaps. |
| Claude Code unavailable | `claude -p ...` returned `Not logged in · Please run /login` | Use Codex sub-agents for this run; authenticate Claude Code before a future required Claude-specific review. |

## Final Completion Claim

Do not write "onboarding complete" unless every mandatory P0/P1/P2/P3/P4 row is
`done` or has a concrete, user-visible `blocked` disposition and this command
passes:

```bash
cargo run -p policy-engine-integration-tests --bin check-onboarding-evidence -- curve --phase all
```
