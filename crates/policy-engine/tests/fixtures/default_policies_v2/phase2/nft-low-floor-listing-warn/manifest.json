{
  "id": "nft-low-floor-listing-warn",
  "schema_version": 2,
  "trigger": { "where": { "action.domain": { "eq": "unknown" } } },
  "policy_rpc": [
    {
      "id": "listing-vs-floor",
      "method": "external.nft_floor",
      "params": {
        "chain_id": "$.root.chain_id",
        "owner": "$.root.from",
        "calldata": "$.action.calldata",
        "target": "$.action.target"
      },
      "outputs": [
        {
          "kind": "context",
          "field": "listingPctOfFloor",
          "type": "Decimal",
          "from": "$.result.orderVsFloor"
        }
      ],
      "optional": true
    }
  ],
  "custom_context": {
    "fields": {
      "listingPctOfFloor": "decimal"
    }
  }
}
