{
  "id": "eu-sanctions-listed-recipient-deny",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "erc20_transfer" } } },
  "policy_rpc": [
    {
      "id": "recipient-eu-sanctions",
      "method": "external.address_blocklisted",
      "params": {
        "chain_id": "$.root.chain_id",
        "address": "$.action.recipient",
        "list": "eu_consolidated_sanctions"
      },
      "outputs": [
        {
          "kind": "context",
          "field": "recipientEuSanctioned",
          "type": "Bool",
          "from": "$.result.blocklisted"
        }
      ],
      "optional": true
    }
  ],
  "custom_context": { "fields": { "recipientEuSanctioned": "Bool" } }
}
