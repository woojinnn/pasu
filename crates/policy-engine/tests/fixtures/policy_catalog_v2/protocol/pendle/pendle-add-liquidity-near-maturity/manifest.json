{
  "id": "pendle-add-liquidity-near-maturity",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "add_market_liquidity" } } },
  "policy_rpc": [{
    "id": "now",
    "method": "clock.now",
    "params": {},
    "outputs": [{ "kind": "context", "field": "nowTs", "type": "Long", "from": "$.result.nowTs", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "nowTs": "Long" } }
}
