{
  "id": "stake-usd-cap",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "stake" } } },
  "policy_rpc": [{
    "id": "stake-usd",
    "method": "oracle.usd_value",
    "params": {
      "chain_id": "$.root.chain_id",
      "asset": "$.action.venue",
      "amount": "$.action.amount"
    },
    "outputs": [{ "kind": "context", "field": "stakeUsd", "type": "Decimal", "from": "$.result.usd", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "stakeUsd": "decimal" } }
}
