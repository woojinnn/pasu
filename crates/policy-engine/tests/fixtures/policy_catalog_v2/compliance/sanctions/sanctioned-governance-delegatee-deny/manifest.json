{
  "id": "sanctioned-governance-delegatee-deny",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "delegate" } } },
  "policy_rpc": [{
    "id": "delegatee-sanctions",
    "method": "address.sanctions",
    "params": { "address": "$.action.delegatee" },
    "outputs": [{ "kind": "context", "field": "sanctioned", "type": "Bool", "from": "$.result.sanctioned", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "sanctioned": "Bool" } }
}
