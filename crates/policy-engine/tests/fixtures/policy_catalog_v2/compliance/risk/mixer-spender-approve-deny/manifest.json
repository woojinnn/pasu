{
  "id": "mixer-spender-approve-deny",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "erc20_approve" } } },
  "policy_rpc": [{
    "id": "spender-category",
    "method": "address.category",
    "params": { "address": "$.action.spender" },
    "outputs": [{ "kind": "context", "field": "riskCategory", "type": "String", "from": "$.result.riskCategory", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "riskCategory": "String" } }
}
