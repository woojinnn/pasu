{
  "id": "usdt-blacklisted-recipient-deny",
  "schema_version": 2,
  "trigger": { "where": { "action.tag": { "eq": "erc20_transfer" } } },
  "policy_rpc": [{
    "id": "recipient-frozen",
    "method": "address.frozen",
    "params": { "asset": "$.action.token", "address": "$.action.recipient" },
    "outputs": [{ "kind": "context", "field": "frozen", "type": "Bool", "from": "$.result.frozen", "required": false }],
    "optional": true
  }],
  "custom_context": { "fields": { "frozen": "Bool" } }
}
