[package]
name = "simulation-sync"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
simulation-state = { workspace = true }
simulation-db = { path = "../db", version = "0.1.0" }
# Action types — sync 가 Action.live_inputs 도 walk/apply 하기 위해
simulation-reducer = { workspace = true }

# 외부 데이터 fetch
tokio = { workspace = true }
reqwest = { workspace = true }
async-trait = { workspace = true }

# Config
toml = { workspace = true }

# Serde
serde = { workspace = true }
serde_json = { workspace = true }

# Errors
thiserror = { workspace = true }

# EVM types (eth_call ABI encoding 등)
alloy-primitives = { workspace = true }
hex = { workspace = true }
